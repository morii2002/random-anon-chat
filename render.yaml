services:
  - type: web
    name: chat-now
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASS
        sync: false
